<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/network_folder_holder"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/tiny_margin"
    android:paddingStart="@dimen/activity_margin"
    android:paddingEnd="0dp">

    <org.fossify.commons.views.MyTextView
        android:id="@+id/network_folder_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/medium_margin"
        android:layout_toStartOf="@+id/overflow_menu_icon"
        android:textStyle="bold"
        tools:text="NAS" />

    <org.fossify.commons.views.MyTextView
        android:id="@+id/network_folder_subtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/network_folder_title"
        android:layout_marginTop="@dimen/tiny_margin"
        android:layout_marginBottom="@dimen/medium_margin"
        android:layout_toStartOf="@+id/overflow_menu_icon"
        android:alpha="0.8"
        tools:text="192.168.1.10 / Public" />

    <ImageView
        android:id="@+id/overflow_menu_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:padding="@dimen/activity_margin"
        android:src="@drawable/ic_three_dots_vector" />

    <View
        android:id="@+id/overflow_menu_anchor"
        style="@style/OverflowMenuAnchorStyle"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true" />

</RelativeLayout>

